---
import Layout from '../../layouts/Layout.astro';

export async function getStaticPaths() {
	const posts = [
		'building-modern-web-applications',
		'future-of-frontend',
		'optimizing-react-performance',
		'python-automation-financial-reporting',
		'file-integrity-monitoring-security',
		'data-pipeline-integration'
	];

	return posts.map((slug) => ({
		params: { slug },
	}));
}

interface Props {
	Astro: {
		params: {
			slug: string;
		};
	};
}

const { slug } = Astro.params;

// In a real app, you'd fetch this from a CMS or markdown files
const posts: Record<string, {
	title: string;
	date: string;
	content: string;
	tags: string[];
	readTime: string;
}> = {
	'building-modern-web-applications': {
		title: 'Building Modern Web Applications',
		date: '2024-01-15',
		readTime: '8 min read',
		tags: ['Web Dev', 'Architecture', 'Best Practices'],
		content: `
			<p>Building modern web applications requires a deep understanding of both the technical and architectural aspects of development. In this post, we'll explore the key patterns and practices that make applications scalable, maintainable, and performant.</p>
			
			<h2>Component Architecture</h2>
			<p>One of the most important aspects of modern web development is component-based architecture. This approach allows for better code organization, reusability, and maintainability.</p>
			
			<h2>State Management</h2>
			<p>Effective state management is crucial for building complex applications. Whether you're using Redux, Zustand, or React's built-in state management, the key is consistency and predictability.</p>
			
			<h2>Performance Optimization</h2>
			<p>Performance should be considered from the start, not as an afterthought. Techniques like code splitting, lazy loading, and memoization can significantly improve application performance.</p>
		`
	},
	'future-of-frontend': {
		title: 'The Future of Frontend Development',
		date: '2024-01-10',
		readTime: '6 min read',
		tags: ['Frontend', 'Technology', 'Trends'],
		content: `
			<p>The frontend development landscape is constantly evolving. Let's explore the emerging technologies and frameworks that are shaping the future of web development.</p>
			
			<h2>Server Components</h2>
			<p>Server components represent a paradigm shift in how we think about component architecture, allowing for better performance and reduced client-side JavaScript.</p>
			
			<h2>Edge Computing</h2>
			<p>Edge computing is bringing computation closer to users, reducing latency and improving performance for global applications.</p>
		`
	},
	'optimizing-react-performance': {
		title: 'Optimizing Performance in React',
		date: '2024-01-05',
		readTime: '10 min read',
		tags: ['React', 'Performance', 'Optimization'],
		content: `
			<p>React performance optimization is an art that requires understanding both React's internals and browser rendering. Here are practical tips to improve your React applications.</p>
			
			<h2>Memoization</h2>
			<p>Use React.memo, useMemo, and useCallback strategically to prevent unnecessary re-renders and computations.</p>
			
			<h2>Code Splitting</h2>
			<p>Implement route-based and component-based code splitting to reduce initial bundle size and improve load times.</p>
		`
	},
	'python-automation-financial-reporting': {
		title: 'Automating Financial Reporting with Python',
		date: '2024-09-15',
		readTime: '12 min read',
		tags: ['Python', 'Automation', 'Data Analysis'],
		content: `
			<p>In my role at Kava Kraze, I developed FinSync Automator, a Python automation tool that transformed our weekly financial reporting process. What used to take 2 hours of manual work now takes less than 5 minutes of verification.</p>
			
			<h2>The Challenge</h2>
			<p>Weekly financial reporting required manually extracting data from Lightspeed POS, reconciling it with QuickBooks, and compiling reports. This process was error-prone and time-consuming.</p>
			
			<h2>The Solution</h2>
			<p>I engineered a Python tool that automates the entire pipeline: data extraction, reconciliation, report generation, and email delivery. The system integrates seamlessly with both platforms and handles edge cases automatically.</p>
			
			<h2>Results</h2>
			<p>The automation eliminated 96% of manual work, reducing processing time from 2 hours to under 5 minutes. This freed up significant time for more strategic analysis and decision-making.</p>
		`
	},
	'file-integrity-monitoring-security': {
		title: 'Building a File Integrity Monitoring System',
		date: '2024-08-20',
		readTime: '15 min read',
		tags: ['Cybersecurity', 'Python', 'HIDS'],
		content: `
			<p>Guardian FIM is a complete File Integrity Monitoring system I designed and engineered as a host-based intrusion detection (HIDS) tool. This project demonstrates my ability to build security tools from the ground up.</p>
			
			<h2>Architecture</h2>
			<p>The system uses an SQLite database to store file-system baselines and track all change history. Integrity checks are performed using SHA256 hashes for high-speed verification and cryptographic security.</p>
			
			<h2>Key Features</h2>
			<p>The modular, configuration-driven design includes file exclusions, comprehensive logging, and multi-format report generation (Console, JSON, HTML). This makes it adaptable to different environments and use cases.</p>
			
			<h2>Testing & Reliability</h2>
			<p>I ensured code reliability and cross-platform compatibility by writing a comprehensive unit and integration test suite. This guarantees the system works consistently across different operating systems.</p>
		`
	},
	'data-pipeline-integration': {
		title: 'Managing Multi-Platform Data Pipelines',
		date: '2024-07-10',
		readTime: '10 min read',
		tags: ['Data Integration', 'E-Commerce', 'Automation'],
		content: `
			<p>At Tress Marketing Solutions, I manage complex data pipelines between HubSpot, SamCart, Shopify, and Post Affiliate Pro. Ensuring data integrity across these platforms is critical for accurate commission payments and reporting.</p>
			
			<h2>The Challenge</h2>
			<p>Multi-million dollar affiliate commission programs require perfect data synchronization. Any discrepancy can lead to incorrect payments, compliance issues, and financial losses.</p>
			
			<h2>My Approach</h2>
			<p>I proactively monitor the end-to-end data pipeline, swiftly resolving discrepancies to ensure data integrity. Daily audits validate payments and ensure accurate affiliate attribution for all transactions.</p>
			
			<h2>Tools & Techniques</h2>
			<p>I develop advanced Google Sheets dashboards for KPI reporting and use Python scripts for automated data validation. This combination provides both real-time visibility and automated quality assurance.</p>
		`
	}
};

const post = posts[slug];

if (!post) {
	return Astro.redirect('/blog');
}
---

<Layout title={`${post.title} - Tyler Pauley`}>
	<article class="post-page">
		<div class="container">
			<header class="post-header">
				<a href="/blog" class="back-link">
					<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<path d="M19 12H5M12 19l-7-7 7-7"></path>
					</svg>
					Back to Blog
				</a>
				<div class="post-meta">
					<time datetime={post.date} class="post-date">
						{new Date(post.date).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}
					</time>
					<span class="post-read-time">{post.readTime}</span>
				</div>
				<h1 class="post-title">{post.title}</h1>
				<div class="post-tags">
					{post.tags.map((tag) => (
						<span class="post-tag">{tag}</span>
					))}
				</div>
			</header>
			<div class="post-content" set:html={post.content} />
		</div>
	</article>
</Layout>

<style>
	.post-page {
		padding: 4rem 2rem;
		min-height: 80vh;
	}

	.container {
		max-width: 800px;
		margin: 0 auto;
	}

	.back-link {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		color: var(--color-text-muted);
		text-decoration: none;
		font-weight: 500;
		margin-bottom: 2rem;
		transition: color 0.2s;
	}

	.back-link:hover {
		color: var(--color-accent);
	}

	.post-header {
		margin-bottom: 3rem;
	}

	.post-meta {
		display: flex;
		gap: 1rem;
		align-items: center;
		margin-bottom: 1rem;
		flex-wrap: wrap;
	}

	.post-date {
		font-size: 0.875rem;
		color: var(--color-text-muted);
		font-family: 'JetBrains Mono', monospace;
	}

	.post-read-time {
		font-size: 0.875rem;
		color: var(--color-text-muted);
	}

	.post-title {
		font-size: clamp(2rem, 5vw, 3rem);
		font-weight: 700;
		line-height: 1.2;
		margin-bottom: 1.5rem;
		color: var(--color-text);
	}

	.post-tags {
		display: flex;
		gap: 0.5rem;
		flex-wrap: wrap;
	}

	.post-tag {
		background: var(--color-surface-elevated);
		border: 1px solid var(--color-border);
		padding: 0.375rem 0.875rem;
		border-radius: 6px;
		font-size: 0.875rem;
		color: var(--color-text-muted);
	}

	.post-content {
		color: var(--color-text);
		line-height: 1.8;
		font-size: 1.125rem;
	}

	.post-content h2 {
		font-size: 2rem;
		font-weight: 600;
		margin-top: 3rem;
		margin-bottom: 1rem;
		color: var(--color-text);
	}

	.post-content h3 {
		font-size: 1.5rem;
		font-weight: 600;
		margin-top: 2rem;
		margin-bottom: 1rem;
		color: var(--color-text);
	}

	.post-content p {
		margin-bottom: 1.5rem;
		color: var(--color-text-muted);
	}

	.post-content code {
		background: var(--color-surface-elevated);
		border: 1px solid var(--color-border);
		padding: 0.125rem 0.5rem;
		border-radius: 4px;
		font-family: 'JetBrains Mono', monospace;
		font-size: 0.9em;
		color: var(--color-accent);
	}

	.post-content pre {
		background: var(--color-surface-elevated);
		border: 1px solid var(--color-border);
		border-radius: 8px;
		padding: 1.5rem;
		overflow-x: auto;
		margin: 2rem 0;
	}

	.post-content pre code {
		background: none;
		border: none;
		padding: 0;
		color: var(--color-text);
	}

	.post-content a {
		color: var(--color-accent);
		text-decoration: none;
		border-bottom: 1px solid transparent;
		transition: border-color 0.2s;
	}

	.post-content a:hover {
		border-bottom-color: var(--color-accent);
	}

	.post-content ul,
	.post-content ol {
		margin: 1.5rem 0;
		padding-left: 2rem;
	}

	.post-content li {
		margin-bottom: 0.75rem;
		color: var(--color-text-muted);
	}

	@media (max-width: 768px) {
		.post-page {
			padding: 3rem 1.5rem;
		}
	}
</style>

